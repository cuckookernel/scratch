[project]
name = "git-ui"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "rich>=14.3.3",
    "textual>=8.1.1",
]

[dependency-groups]
dev = [
    "ruff>=0.15.5",
]
