(executable
 (name expressions)
 (libraries base str oUnit))

(alias
  (name    runtest)
  (deps    (:x expressions.exe))
  (action  (run %{x})))

(alias
  (name    buildtest)
  (deps    (:x test.exe)))

(env
  (dev
    (flags (:standard -warn-error -A))))