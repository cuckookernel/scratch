This subproject assumes that there is a symbolic link to the data dir at contar_palmas/_data (relative to repo root), 
for example (assuming repo root is is at `~/git/_personal/scratch/`)

```bash
cd ~
ln -s _data/personal_data/contar_palmas  ~/git/_personal/scratch/contar_palmas/_data
```
